title: CrawlSage
description: An F#-first web crawling & scraping framework for .NET.
remote_theme: pages-themes/cayman@v0.2.0
plugins:
  - jekyll-remote-theme
  - jekyll-seo-tag

# baseurl / url are injected automatically by actions/configure-pages — do not hardcode.
